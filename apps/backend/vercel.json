{
    "version": 2,
    "builds": [
      {
        "src": "/",
        "use": "@vercel/node",
        "config": {
           "buildCommand": "ls && npm run build"
        }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/"
      }
    ]
  }